Multiple-Choice Questions on Specifications for AI-Assisted Development

- Module 3 quiz, version 1.0

---

1 What is the primary purpose of a specification in engineering?
1. _**To describe what the system must do without prescribing how to build it**_
2. To document the implementation choices made by the developer
3. To enumerate the bugs found during testing
4. To record the personnel and budget assigned to a project
5. Do not know

---

2 Which statement best distinguishes a specification from a design?
1. A specification is for managers; a design is for engineers
2. A specification describes structure; a design describes behavior
3. _**A specification describes what the system must do; a design describes how it will be structured**_
4. A specification is informal; a design is always formal
5. Do not know

---

3 A requirement is considered unambiguous when:
1. _**It has exactly one interpretation when read by different stakeholders**_
2. It is written by a single author
3. It uses only technical vocabulary
4. It is shorter than fifty words
5. Do not know

---

4 Why does a clear specification typically reduce the cost of long-term software maintenance?
1. It allows the original developers to be reassigned immediately
2. It prevents the need for further changes to the software
3. It guarantees backward compatibility with all future versions
4. _**It captures the intent behind the code, which would otherwise have to be reverse-engineered by future maintainers**_
5. Do not know

---

5 Why does an AI coding assistant generally require a more explicit specification than a senior team member would?
1. The AI assistant ignores any instructions shorter than 100 words
2. _**The AI assistant has no persistent project memory and lacks the implicit context a long-tenured team member would have absorbed**_
3. The AI assistant cannot read code that is more than 50 lines long
4. The AI assistant only accepts requirements written in EARS notation
5. Do not know

---

6 When an AI assistant receives an underspecified prompt for code generation, which of the following is the most concerning default behavior?
1. Returning an empty response
2. Asking the user a hundred clarifying questions
3. _**Inventing API calls or library functions that do not actually exist (hallucination)**_
4. Compiling the request into machine code automatically
5. Do not know

---


7 The discipline of writing a good specification for an AI assistant is:
1. _**Essentially the same skill as writing a specification for a human reader, with extra explicit context that a teammate would have absorbed implicitly**_
2. Fundamentally different from writing a specification for a human reader
3. Required only when using Copilot, not when using other AI assistants
4. Optional because the AI will infer the requirements automatically
5. Do not know

---

8 In the recommended Copilot setup, what is the purpose of the file `.github/copilot-instructions.md`?
1. To list every chat session ever held in the repository
2. _**To hold repository-wide instructions that Copilot reads automatically in every chat in that repository**_
3. To store API keys and credentials for Copilot to use
4. To document the project's release history
5. Do not know

---
9 Prompt files under `.github/prompts/` are best used for:
1. _**Defining reusable task templates that can be invoked by name as slash commands**_
2. Storing copies of every prompt the team has ever typed
3. Replacing the version control system
4. Configuring the IDE's keyboard shortcuts
5. Do not know

--- 

10 Which of the following is the best argument for writing a specification BEFORE prompting an AI assistant for a non-trivial coding task?
1. The AI will refuse to respond without one
2. _**The time spent writing the spec is typically less than the time spent reworking unspecified code, and the discipline exposes ambiguities in the developer's own thinking**_
3. Specifications are required by the GitHub terms of service
4. The IDE will not function without a spec attached
5. Do not know

---


## End
